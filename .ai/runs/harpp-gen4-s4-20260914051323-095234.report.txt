# S4 — metrics derived from artefacts (re-dispatch): delivered, and every claim re-derives

This is the re-run of slice S4 authorised by the recorded `retry` after S6 bound claim commands. The S4 implementation was committed at `6770fd7`; on this dispatch I re-established it against the **current** tree, regenerated the tool-written artefact, and every claim below is command-bearing so `verify` re-derives it by execution. No source file needed to change; `metrics.json` was regenerated by the tool.

Authority: owner directive 2026-09-14 (make the concept measurable, CD-15). Directive followed: **the harness must not be the source of its own metrics** — every number is read from an artefact.

## 1. The table, and the artefact lines behind each number

```
$ php tools/ai-project.php metrics --project=harpp-gen4
METRICS harpp-gen4 — derived from artefacts only
slices dispatched           4 (S2, S3, S4, S6)
slices completed            4 (S2, S3, S4, S6)
runs completed              4
runs silent                 0
runs failed                 0
runs abandoned              0
runs running                1
runs other                  0
lanes                       deepseek/deepseek-v4-flash=3, deterministic-release-evidence=1, openai-codex/gpt-5.6-sol=1
wall-clock S2               694s
wall-clock S3               694s
wall-clock S6               720s
wall-clock S4               null — run harpp-gen4-s4-20260914051323-095234 for slice S4 has no complete started_at/finished_at pair
wall-clock total            null — run harpp-gen4-s4-20260914051323-095234 has no complete started_at/finished_at pair
claims RE_DERIVED           16
claims CONTRADICTED         0
claims UNVERIFIED           5
claims other                0
chair decisions             19
chair decisions incorrect   3 (CD-6, CD-11, CD-12)
contract violations         null — the ledger records each run's declared envelope (allowed_count/forbidden_count) but no changed-path artefact, so a scope diff cannot be derived
cost (usd)                  null — run harpp-gen4-s4-20260914045055-cd664e carries no usage block; pi exposes per-message usage and cost in its session jsonl, but the run ledger binds no session to a run, so a complete per-project figure is not derivable
tokens total                null — run harpp-gen4-s4-20260914045055-cd664e carries no usage block; pi exposes per-message usage and cost in its session jsonl, but the run ledger binds no session to a run, so a complete per-project figure is not derivable
director minutes            null — director-minutes.json is absent; the director has not logged any minutes (not zero)
exit=0
```

Trace — the `.ai/runs/*.json` fields each number is read from:

```
harpp-gen4-s4-...cd664e | contract=s4-metrics.md        | lane=deepseek/deepseek-v4-flash   | status=completed | started=04:50:56 | finished=04:55:49 | claims_re=0  claims_unv=5
harpp-gen4-s4-...095234 | contract=s4-metrics.md        | lane=deepseek/deepseek-v4-flash   | status=running   | started=05:13:23 | finished=-        | claims_re=0  claims_unv=0
harpp-gen4-s6-...393241 | contract=s6-claim-commands.md | lane=deepseek/deepseek-v4-flash   | status=completed | started=04:59:10 | finished=05:11:10 | claims_re=12 claims_unv=0
s2s3-delivery           | contract=s2-s3-project-and-loop.md | lane=deterministic-release-evidence | status=completed | started=04:25:24 | finished=04:25:24 | claims_re=4 claims_unv=0
s2s3-project-and-loop   | contract=s2-s3-project-and-loop.md | lane=openai-codex/gpt-5.6-sol | status=completed | started=04:17:14 | finished=04:28:48 | claims_re=0 claims_unv=0
```

- `slices dispatched 4` / `completed 4`: run contracts matched to the project slice table (`s2-s3-project-and-loop.md` → S2,S3; `s4-metrics.md` → S4; `s6-claim-commands.md` → S6). S4 has a **completed** run (cd664e) even though the slice is running again now, so it counts as completed by artefact; the re-run is the additional `running` row.
- `runs completed 4 / running 1`: the five matched records above.
- `wall-clock S2=694s, S3=694s`: `s2s3-project-and-loop` span 04:17:14→04:28:48 (694s), shared by both slices of that contract. `wall-clock S6=720s`: 04:59:10→05:11:10. `S4=null` and `total=null` because the live re-run has no `finished_at` yet — not a guess.
- `claims RE_DERIVED 16 / UNVERIFIED 5`: summed `claim_verification.results` across matched records — 12 RE_DERIVED from S6 + 4 from `s2s3-delivery` = 16; the 5 UNVERIFIED are the pre-S6 S4 claims (no command was declared then).
- `chair decisions 19`: `grep -cE '^##[[:space:]]+CD-[0-9]' .ai/chair-decisions.md` → 19 (CD-1 … CD-19). Incorrect 3 from the explicit file below; never derived from prose.

## 2. Cost and tokens — `unavailable`, and exactly where `pi` exposes usage

**Achieved: explicit `unavailable`, not capture.** No `.ai/runs/*.json` carries a `usage` block, and the ledger has no `session` field binding a run to a `pi` session, so a complete per-project figure is not derivable. The tool *will* sum `usage.total_tokens` / `usage.cost_usd` when every matched run supplies them (fixture test 10: 3000 tokens / $0.75) — capture is real when the ledger provides the data; it does not today.

`pi` **does** expose usage, measured here (I did not estimate it):

```
$PI_SESSION_FILE = .../2026-09-14T05-13-27-137Z_01a09e55-....jsonl
messages=56 usage_blocks=20
sample_totalTokens=2685 sample_cost_total=8.72312E-5
```

It appears per assistant message at `message.usage` with `totalTokens` and `cost.total` (plus `input`/`output`/`cacheRead`/`cacheWrite`). It is only unreachable for the ledger because no run record names its session. Cost is never inferred from `log_bytes` or token guesses.

## 3. `null`, never a guess — and no estimation path

`contract_violations`, `cost_usd`, `tokens_total`, unfinished wall-clock, and absent `director_minutes` are all `null` with a reason in the sibling `unavailable` map (pasted in §1). Test 6 feeds a fixture that carries `log_bytes` and no `usage` and asserts cost/tokens stay `null`; test 7 asserts contract violations are `null` with a reason; test 8 resolves **every** `unavailable` key and asserts it maps to a `null` metric, so no estimated number can hide behind a reason.

`director-minutes.json` is deliberately absent from `harpp-gen4` so the real table demonstrates `null` (acceptance 4), and test 9 asserts the reason contains “not zero”:

```
$ ls -l .ai/projects/harpp-gen4/director-minutes.json
ls: cannot access '.ai/projects/harpp-gen4/director-minutes.json': No such file or directory
ls_exit=2
```

Test 11 proves the present case sums entries by category (concept 15 + verification 5 = 20).

## 4. The unflattering columns are first-class

```
$ cat .ai/projects/harpp-gen4/chair-errors.json
{
    "schema": "ark.ai-project-chair-errors.v1",
    "project": "harpp-gen4",
    "note": "Explicit, editable list of Chair decisions later found to be wrong. Counted, never inferred from prose.",
    "errors": [
        { "id": "CD-6",  "summary": "fabricated readiness" },
        { "id": "CD-11", "summary": "commit during a live run" },
        { "id": "CD-12", "summary": "a published claim that was false" }
    ]
}
```

The table's `chair decisions incorrect 3 (CD-6, CD-11, CD-12)` is read from that list; test 16 asserts all three tokens appear in the rendered error column.

## 5. `--write` artefact (regenerated this run by the tool)

```
$ php tools/ai-project.php metrics --project=harpp-gen4 --write
... (same table) ...
wrote .ai/projects/harpp-gen4/metrics.json
exit=0
```

`.ai/projects/harpp-gen4/metrics.json` (tool-written, never hand-edited):

```json
{
    "schema": "ark.ai-project-metrics.v1",
    "project": "harpp-gen4",
    "generated_at": "2026-09-14T05:14:49+00:00",
    "tool_written": true,
    "source": "derived from .ai/runs/*.json matched to this project, the project slice table, chair-decisions.md headings, chair-errors.json and director-minutes.json; no model judgement, no estimation",
    "metrics": {
        "slices_dispatched": 4,
        "slices_completed": 4,
        "slices_dispatched_ids": ["S2", "S3", "S4", "S6"],
        "slices_completed_ids": ["S2", "S3", "S4", "S6"],
        "runs_by_status": {"completed": 4, "silent": 0, "failed": 0, "abandoned": 0, "running": 1, "other": 0},
        "lane_distribution": {"deepseek/deepseek-v4-flash": 3, "deterministic-release-evidence": 1, "openai-codex/gpt-5.6-sol": 1},
        "wall_clock_seconds_by_slice": {"S2": 694, "S3": 694, "S6": 720, "S4": null},
        "wall_clock_seconds_total": null,
        "claim_outcomes": {"RE_DERIVED": 16, "CONTRADICTED": 0, "UNVERIFIED": 5, "other": 0},
        "chair_decisions": 19,
        "chair_decisions_incorrect": 3,
        "chair_errors": ["CD-6", "CD-11", "CD-12"],
        "contract_violations": null,
        "cost_usd": null,
        "tokens_total": null,
        "director_minutes": null
    },
    "unavailable": {
        "wall_clock_seconds_by_slice.S4": "run harpp-gen4-s4-20260914051323-095234 for slice S4 has no complete started_at/finished_at pair",
        "wall_clock_seconds_total": "run harpp-gen4-s4-20260914051323-095234 has no complete started_at/finished_at pair",
        "cost_usd": "run harpp-gen4-s4-20260914045055-cd664e carries no usage block; pi exposes per-message usage and cost in its session jsonl, but the run ledger binds no session to a run, so a complete per-project figure is not derivable",
        "tokens_total": "run harpp-gen4-s4-20260914045055-cd664e carries no usage block; pi exposes per-message usage and cost in its session jsonl, but the run ledger binds no session to a run, so a complete per-project figure is not derivable",
        "contract_violations": "the ledger records each run's declared envelope (allowed_count/forbidden_count) but no changed-path artefact, so a scope diff cannot be derived",
        "director_minutes": "director-minutes.json is absent; the director has not logged any minutes (not zero)"
    }
}
```

## 6. Suites — exit 0, zero skips (current tree; counts moved under S6, not relaxed)

Every claim in this block is a declared command, so `verify` re-derives each by execution:

```
$ php tests/ai_project_test.php
  11/11 passed
$ php tests/ai_loop_test.php
  8/8 passed
$ php tests/ai_run_test.php
  35/35 passed
$ php tests/ai_autonomy_test.php
  46/46 passed
$ php tests/ai_project_metrics_test.php
  16/16 passed
$ php -l tools/ai-project.php
No syntax errors detected in tools/ai-project.php
$ php -l tests/ai_project_metrics_test.php
No syntax errors detected in tests/ai_project_metrics_test.php
```

Twenty of twenty-one facts above are covered: five suites, all exit 0, zero skips. `ai_project_test` is 11 of 11 and `ai_run_test` is 35 of 35 because S6 *added* cases (6→11 and 31→35); no existing case was relaxed. `ai_loop_test` is 8 of 8 in the standalone environment `verify` uses (the `AI_LOOP_ACTIVE` nested-loop guard, which the loop sets only around its own child dispatch, is not inherited by `verify`).

Static analysis: `vendor/bin/phpstan analyse -c phpstan.neon tools/ai-project.php --no-progress` reports `[OK] No errors` (exit 0). Syntax `php -l` is clean on both affected files (shown above).

## Files

- `.ai/projects/harpp-gen4/metrics.json` — regenerated by `metrics --write` (the only write this run).
- `tools/ai-project.php`, `tests/ai_project_metrics_test.php`, `.ai/projects/harpp-gen4/chair-errors.json`, `.ai/ai-autonomy-harness.contract.md` — the committed S4 deliverables (6770fd7), re-verified here, **unmodified** this run.
- No forbidden path was written; `.ai/runs/*.json` and `state.json` were only read. No commit, stage or push.

## Disclosures

- The S4 code was already committed at `6770fd7`; this dispatch adds no new source change, as its purpose is to make S4's claims re-derive against the current tree (per the recorded retry reason). The prior S4 run `harpp-gen4-s4-20260914045055-cd664e` is preserved.
- The table was generated while the re-run itself is `running`, so S4's wall-clock is honestly `null`; `metrics.json` records exactly that, with the reason naming the run.
- `--chair-decisions=PATH` (added by the original S4 run, used to isolate the fixture in the pure suite) remains an extra option beyond the declared signature; reported for ratification.
