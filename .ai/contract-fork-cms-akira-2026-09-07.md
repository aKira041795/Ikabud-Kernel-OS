task: CMS Akira fork — Architectural Reset / Clarification, milestone "Presentation Contract Reset": prove ONE canonical Post path (domain truth → capability → Entity View semantic firewall → ARK renderer selection → DiSyL → HTML) with no ambiguous ownership and no duplicate registries. Fork-owned minimal Post content authority replaces the inert `cms` delegation. Feature development frozen; this deliverable is a contract, no file edits.

objective: Make `cms-akira-core` the self-owned Post content authority (table `cms_akira_posts` + `cms.post.*@1` capabilities, provider `cms-akira-core`); register `entity.list.post` + `entity.detail.post` as explicit-field (no `'*'`) view contracts with a fail-closed required-registration guard; ship a minimal ARK theme mapping `entity.list.post`→`article-grid` and `entity.detail.post`→`article-page` via a single `renderer-registry.json`; record the Entity View / ARK / DiSyL / Builder ownership ADR BEFORE any ARK implementation (P3 only ratifies/freezes); prove Kernel 6.x post-success invalidation on `cms.post.update@1` with durable idempotency + independent audit evidence.

scope:
  allowed:
    - P1 — Canonical Post READ/RENDER path (the ONLY Phase-1 build; no mutation in P1):
        - CONTENT AUTHORITY (gap A, DECIDED): module-id = `cms-akira-core` (existing product-core host becomes the fork content authority — no new module). Domain capability ids = `cms.post.get@1`, `cms.post.list@1`, `cms.post.create@1`, `cms.post.update@1`, provider = `cms-akira-core`. Rationale: legacy `cms` is intentionally absent, so the `cms.post.*` namespace is vacant and claimed by the fork; capability-id prefix ≠ module ownership (ownership = provider column). Delete `akira.content.*` adapters and remove `depends:["cms"]` plus capability-depends `cms.content.get/list/create/update@1` and `cms.themes.list@1`. Rejected `akira.post.*`: a third namespace with no benefit and contradicts "CMS Core = content authority".
        - POST STORAGE: single tenant-scoped table `cms_akira_posts`, added to `cms-akira-core` `owns_tables` AND `reads_tables` (both currently empty — VERIFIED). Migration lives in `cms-akira-core`'s existing `migrations` slot.
        - DOMAIN READ CAPABILITIES (P1): implement `cms.post.get@1` + `cms.post.list@1` (published-only for public calls), tenant identity from kernel context only.
        - ENTITY VIEW BRIDGE (gap B + critique 1 + critique 2): register bridge handlers under the EXACT unversioned ids `entity.list.post` and `entity.get.post`, single provider `cms-akira-core`, single handler each. NO versioned manifest entries, NO alias/version-resolution dependency. (VERIFIED: `EntityViewResolver::resolve()` calls `entity.list.{type}` and only falls back to `@1`; `resolveDetail()` calls `entity.get.{type}` with NO `@1` fallback.) The bridge constructs fresh allowlisted DTOs: it maps `slug`→`url` (via `action_urls` `/posts/{slug}`), and discards `id`, `tenant_id`, `status`, all internal columns, and every undeclared key before anything reaches ARK/DiSyL. Transport-only lookup keys (the slug passed to `resolveDetail()`, auto-appended `id`/`key_field` in `resolve()`) are consumed inside the bridge and are NEVER part of the ARK-visible contract. Slug is treated as a tenant-scoped slug, never as a numeric id.
        - ENTITY VIEW CONTRACTS (gap B): `registerView('post','list', …)` and `registerView('post','detail', …)`, provider `cms-akira-core`; explicit `fields` allowlist per view (list: title, subtitle, image, metadata, actions, url; detail: title, subtitle, image, body, metadata, actions, url); no `'*'`, no internal/tenant/provider columns; `actions:['view']`; `key_field:'slug'`; `action_urls` for `/posts/{slug}`; `source_schema` owned by `cms-akira-core` with role types declared (title/subtitle/image/body/metadata/actions/url) using the kernel-supported `field_contracts`/`source_schema` keys — no invented `role_fields` key. Rendering ONLY through kernel `EntityViewResolver` + `DefaultEntityRenderer`.
        - ARK AUTHORITY + THEME (gap C): record the ownership ADR (below) BEFORE theme work; then ship a minimal fork theme with `renderer-registry.json` mapping `entity.list.post`→`article-grid` and `entity.detail.post`→`article-page` (template-or-component + `controls` + `context_keys`), plus `tokens.json`.
        - DiSyL (gap D): `article-grid.disyl` + `article-page.disyl` reachable from `renderer-registry.json`; renderers consume ONLY the projected presentation contract. Single ARK authority = `renderer-registry.json`; single view authority = `EntityViewResolver`.
        - ROUTES: `/posts` and `/posts/{slug}`, with fail-closed registration guard BEFORE resolve (see constraints).
    - P2 — Kernel 6.x mutation + invalidation proof (gate B; no mutation capabilities are implemented in P1):
        - `cms.post.create@1` + `cms.post.update@1` (permitted roles `['admin']`; `editor` deferred — no editor provider in P1/P2), registered in the Entity Authority registry and governed by `CapabilityAuthorizationRegistry` so the bus re-authorizes direct calls.
        - `effects.invalidates: ["entity.list.post"]` — ONE canonical tag (critique 6). VERIFIED: regex `^entity\.(?:list|detail)\.(\S+)$` captures `post`; `invalidateEntityCache('post', tenant)` invalidates BOTH `entity.list.post` and `entity.detail.post` in one call. Declaring both would fan out twice.
        - Durable idempotency (corrected per critique 4) + `correlation_id` written to durable audit independently of optional `CapabilityBus` trace logging.
    - P3 — ARK authority ADR ratify/freeze (lock the MAY/MAY-NOT model below) before any ARK surface beyond the two mappings.
    - P4 — DEFERRED MARKER (no build now): builder-as-composition-editor reconnect over a shared ARK engine.
  prohibited:
    - NO second Entity View model, NO second canonicalizer, NO second renderer registry; NO bypass of kernel EntityViewResolver/DefaultEntityRenderer/CapabilityBus; NO kernel edits.
    - NO copying/restoring legacy `cms`; NO delegation to `cms.*`, `theme-studio`, `tinymce`, or `search`.
    - NO DDL beyond the fork-owned `cms_akira_posts` table; NO non-Post entities; NO Theme Studio / page builder / 50 blocks / builder profiles beyond P1 needs.
    - NO media/seo/ai/workflow/search/editor providers; NO enabling the other 13 cms-akira submodules.
    - NO cross-module DB access; NO ARK-owned business entities/DB queries/authorization/business calculations/module capabilities/entity truth; NO Entity View becoming a theme system, page builder, content model, query engine, authorization layer, layout engine, or ARK registry.
    - NO `'*'` field fallback on either Post view; NO generic-fallback render of internal fields on Post routes.
    - NO mutation capability implemented/acceptance-tested in P1; NO `cms.post.create/update@1` handlers activated before P2 gate.
    - NO-BROADEN into kernel or other modules (stabilization baselines stay untouched).

constraints:
    - OWNERSHIP ADR (recorded BEFORE P1 ARK work; P3 ratifies/freezes):
        - Entity View MAY own: semantic field projection, role mapping (title/subtitle/image/body/metadata/actions/url), action presentation metadata, URL projection, presentation-safe normalization.
        - Entity View MAY NOT own: persistence, domain validation, querying, authorization decisions, themes, layout, renderer selection, or ARK registrations.
        - ARK MAY own: renderer selection, tokens, theme layouts, slots, controls, context keys, customization schema.
        - ARK MAY NOT own: Post records, SQL, tenant resolution, permissions, capabilities, workflow/business calculations, or canonical entity data.
        - DiSyL: execution only — no data access or authority decisions.
        - Builder: deferred composition editor using shared ARK contracts.
    - FAIL-CLOSED (module-level, no kernel change): `EntityViewResolver::viewContract()` does NOT fail closed for unregistered views (VERIFIED: ultimate fallback returns generic `fields:'*'`; `DefaultEntityRenderer` caps at `SAFE_FALLBACK_FIELDS`). Therefore `/posts` and `/posts/{slug}` MUST verify the exact `post.list` / `post.detail` registration exists with provider `cms-akira-core` (via `registeredViewContracts()`) BEFORE resolving; otherwise 404/error without invoking generic fallback. Tests prove missing/wrong-provider registration cannot reach ARK/DiSyL and `'*'`/wildcard never renders on either Post route.
    - PROJECTION BOUNDARY (critique 2, executable): the `entity.list.post`/`entity.get.post` bridge returns a fresh DTO containing ONLY the allowlisted contract keys; `url` is synthesized from `slug`; `id`/`tenant_id`/`status`/internal columns and all undeclared keys are dropped. Negative list and detail tests inject a Post carrying extra internal columns and assert they are absent from the ARK/DiSyL input and HTML output.
    - TENANT STORAGE (critique 3, RESOLVED): authoritative tenant identifier = `kernel_tenants.id` — `INT UNSIGNED NOT NULL AUTO_INCREMENT` (VERIFIED `migrations/002_control_plane_tenants.sql`). Therefore `cms_akira_posts.tenant_id INT UNSIGNED NOT NULL`. NO FK to `kernel_tenants` (control-plane table lives in the CONTROL DB, not tenant DBs). Index policy: `UNIQUE KEY uq_tenant_slug (tenant_id, slug)`, `KEY idx_tenant (tenant_id)`. `Idempotency::claim()` requires a positive int tenant (VERIFIED) — satisfied. Every get/list/create/update query obtains tenant identity from trusted kernel context only and includes it in the predicate; payload/query-string tenant ids are rejected/ignored. MySQL 5.7-safe: `CREATE TABLE IF NOT EXISTS`, indexes inline in CREATE, no CTE/window/JSON_TABLE/generated-column edge syntax, `utf8mb4_unicode_ci` (not `utf8mb4_0900_ai_ci`). Provisioning order: table migration → capability/authority registrations → bridge handlers → Entity View registrations → theme validation/activation → routes; any gate failure prevents route activation.
    - IDEMPOTENCY/EVIDENCE (critique 4, CORRECTED): `Idempotency::claim()`/`commit()`/`release()` manage a connection-scoped advisory `GET_LOCK` + `kernel_idempotency_keys` persistence and do NOT provide an atomic transaction spanning the Post write, audit row, and idempotency outcome; `commit()` releases the lock in its `finally` (VERIFIED). P2 therefore requires an EXPLICIT sequence on ONE PDO connection: `BEGIN` → `claim()` (advisory lock; payload-hash conflict → ROLLBACK, 409; duplicate → ROLLBACK, return stored outcome; in_progress → ROLLBACK, 425/retry) → Post write + durable audit row (`correlation_id`) → `commit()` (persist outcome) → `COMMIT`. Crash/rollback behavior: any exception before `COMMIT` → `ROLLBACK` + `release()`; if execution outcome is uncertain (side effect already durable), the claim stays `processing` and is NEVER auto-reclaimed. Document the remaining limitation (write+audit+outcome atomicity is caller-managed via the single-connection transaction, not primitive-guaranteed) and gate it for P2 tests. Mandatory tests: concurrent same-key (one write), payload-hash conflict, post-write failure (no commit, claim released), audit-failure (rollback, no orphaned completed idempotency row).
    - AUTHORIZATION/QUERY (critique 5, executable): `cms.post.create@1`/`cms.post.update@1` permitted roles = `['admin']` only (explicitly; `editor` deferred). Mutations register a governed policy in `CapabilityAuthorizationRegistry` (protocol v2) so the bus enforces authorization on direct calls; reads (`cms.post.get@1`/`list@1`) return only `status='published'` unless an authorized preview/admin policy applies (not built in P1/P2). Handlers derive actor and tenant SOLELY from kernel context (`kernel_request_context` / `app()->user()` / `app()->tenant()->current()`); the module MUST NOT parse/mint an independent JWT or accept actor/role/tenant claims from payloads. HTTP mutation endpoints are non-GET and enforce the kernel CSRF mechanism for session/browser requests. List: `sortable_fields` allowlist (`published_at`, `created_at`, `title`), direction `asc|desc` only; `limit` bounded 1..50, `offset >= 0`; caller-supplied `status`/`filters` are rejected/ignored (published forced) so callers cannot inject `status` to bypass published-only. Tests: direct `CapabilityBus` denial (unauthenticated/unauthorized role), tenant-spoofing, unpublished-content, JWT-claim injection (ignored), browser-CSRF on mutation routes.
    - BRIDGE NAMING (critique 1, RESOLVED): canonical capability ids are the unversioned `entity.list.post` and `entity.get.post` (the exact ids `EntityViewResolver` invokes — VERIFIED); each maps to the SAME single `cms-akira-core` handler. Rejected: versioned `entity.*.post@1` entries + alias/version-resolution (hypothetical, unverified, and dead-ends the detail path). Activation test: `entity.list.post` and `entity.get.post` both resolve to the `cms-akira-core` provider, single handler.
    - INVALIDATION (critique 6): declare exactly ONE effect tag, `entity.list.post`; document fan-out (regex capture `post` → `invalidateEntityCache('post', tenant)` → both `entity.list.post` + `entity.detail.post` fragment tags invalidated once). Invalidation asserted only after successful commit; `CapabilityBus` invalidation is fail-open (VERIFIED), so a freshness test plus an operational warning/metric on failed invalidation are mandatory.
    - Capabilities designed before routes: capability ids + authority registry + `effects.invalidates` declared before `/posts` routes exist.
    - Kernel 7 deferred; build against Kernel 6.x five guarantees (authority, tenant, execution, consistency, evidence) as-is.
    - UNVERIFIED (discovery during implementation, not architecture blockers): exact CLI names for dependency-check / capability-audit / workbench-audit / theme-validate; `DefaultEntityRenderer` safe-fallback internals beyond `SAFE_FALLBACK_FIELDS`; ARK theme-root path resolution beyond `$themeDir.'/'.ltrim($template,'/')`.

acceptance:
    - P1: `/posts` renders via `entity.list.post` and `/posts/{slug}` via `entity.detail.post` through capability → `EntityViewResolver` → ARK renderer selection (`renderer-registry.json`) → `article-grid`/`article-page` DiSyL → HTML, with ONLY explicit allowlisted fields and zero internal/tenant/provider leakage (negative test on a Post carrying extra internal columns).
    - Projection boundary proven: bridge DTOs contain exactly the allowlisted keys; `slug`→`url` mapped; `id`/`tenant_id`/`status`/undeclared keys absent from ARK/DiSyL input and HTML (list + detail).
    - Registration guard: missing/unregistered `post.list`/`post.detail` (or non-`cms-akira-core` ownership) → 404/error, never generic fallback; `'*'`/wildcard never reaches either Post route.
    - Bridge activation: `entity.list.post` and unversioned `entity.get.post` both resolve to the `cms-akira-core` provider (single handler).
    - ARK theme validates clean (`renderer-registry.json` entries have template-or-component, controls, context_keys; templates exist; `tokens.json` present); no duplicate entity-view registry anywhere in the fork.
    - Kernel dependency/activation checks pass for the P1 subset (`cms-akira-core` + minimal ARK theme); `cms.*` / legacy-cms dependencies are gone.
    - P2 (separate gate): `cms.post.update@1` through `CapabilityBus` → `effects.invalidates` (single `entity.list.post`) → `invalidateEntityCache('post', tenant)` → next ARK render fresh (cache-then-mutate-then-render test); durable idempotency (same key = one write; replay returns stored outcome); concurrent same-key = one write; payload-hash conflict rejected; failed write does NOT invalidate; `correlation_id` present in durable audit independently of trace-log env.
    - `capability:audit` and Workbench baselines remain ZERO-exception (P1 and P2 gates).
    - P3 ADR ratified before any ARK surface beyond article-grid/article-page; P4 remains a deferred marker only.

verification:
    - Dependency/activation check for P1 subset (exact command UNVERIFIED; e.g. `php ikabud module:check cms-akira-core`).
    - `php ikabud theme:validate <ark-theme-slug>` → zero errors/warnings (command name UNVERIFIED; doc-claimed in `.ai/ark-contract.md`).
    - HTTP smoke: `GET /posts` and `GET /posts/{slug}` → 200, explicit-field HTML, no internal fields; missing-registration fixture → 404/error; wrong-provider fixture → 404/error.
    - Bridge activation test: `entity.list.post` and unversioned `entity.get.post` both resolve to `cms-akira-core` (single handler); `entity.get.post@1` is NOT relied upon.
    - Projection negative tests (list + detail): extra internal columns and `id`/`tenant_id`/`status` are absent from DTO and HTML.
    - `php ikabud capability:audit` (UNVERIFIED) → baseline unchanged, zero exceptions; Workbench audit (UNVERIFIED command) → zero-exception baseline.
    - P2 regression: mutation invalidates list+detail fragment cache for the correct tenant only, exactly once (single effect tag); idempotency replay returns same outcome with no second write; concurrent same-key = one write; payload-conflict rejected; post-write failure leaves no completed idempotency row; failed write does NOT invalidate; `correlation_id` appears in durable audit independently of trace-log env.
    - Migration rerun/idempotency on MySQL 5.7 dialect (no CTE/window/JSON_TABLE); composite `(tenant_id, slug)` uniqueness enforced; tenant A/B fixtures cover get-by-slug, list, update, idempotency replay, and cache invalidation.
    - Authorization tests: direct `CapabilityBus` mutation denial (unauthenticated / non-admin), tenant-spoofing rejected, unpublished Post hidden on public reads, JWT-claim injection ignored, browser-CSRF enforced on mutation routes.

risk:
    - `cms.post.*` namespace reuse could be misread as legacy `cms` ownership — mitigated by explicit ADR + provider column `cms-akira-core` + removal of all `cms.*` depends.
    - Safe-fallback regression: kernel ultimate fallback uses `fields:'*'` (VERIFIED) — mitigated by required-registration guard BEFORE resolve + fail-closed route test + no `'*'` on Post views.
    - Bridge naming: resolved by registering unversioned `entity.list.post`/`entity.get.post` only (no `@1` dependency on the detail path); mandatory bridge activation test remains.
    - Projection leakage: `EntityViewResolver` passes fields to list but does not strip extras, and passes no allowlist to detail (VERIFIED) — mitigated by the bridge DTO construction + negative list/detail tests.
    - ARK renderer template-path/component mismatch yields validator warnings — must resolve before gate.
    - Stale-cache / duplicate-invalidation: single canonical `entity.list.post` effect tag avoids double fan-out; invalidation itself is fail-open (VERIFIED) — covered by P2 cache-then-mutate test + operational warning/metric on failed invalidation.
    - Tenant leakage on Post reads/mutation — covered by tenant A/B fixtures and tenant-identity-from-kernel-context-only rule; `tenant_id INT UNSIGNED` matches `kernel_tenants.id` (VERIFIED).
    - Idempotency atomicity: primitive does not span write+audit+outcome (VERIFIED) — mitigated by documented single-connection transaction sequence, rollback/crash behavior, and P2 concurrency/failure tests; residual limitation explicitly documented.
    - Deferred 13 submodules may fail activation if their manifests still depend on removed `akira.content.*` — excluded from the P1/P2 gates; re-check only when individually re-enabled (P4+).
    - UNVERIFIED operational commands / theme-root path resolution / `DefaultEntityRenderer` internals — confirmed during implementation; none are architecture blockers.

status: READY_FOR_IMPLEMENTATION

## P1 implementation preflight (2026-09-07)
- **Theme root / lifecycle:** kernel `App::arkRenderers()` resolves from `CMS_THEMES_PATH`, falling back to `STORAGE_PATH/cms-themes`; the P1 source theme slug is `cms-akira-posts` at `storage/cms-themes/cms-akira-posts/`. Deploy the source directory before activation, validate it, then pass the request's trusted `active_theme_slug`; when none is available P1 explicitly defaults to `cms-akira-posts`. No compiled/public build artifacts are deployed beyond source DiSyL templates.
- **Validator CLI:** `php ikabud theme:validate cms-akira-posts` is the discovered command and passes with zero warnings/errors. Module migrations for this nested suite use `php ikabud migrate cms-akira/cms-akira-core` (not the bare module id).
- **Tenant provisioning evidence:** `TenantProvisioner::provision()` connects each tenant DB, runs `tenantRunCoordinatedProvisionMigrations()`, verifies, then calls `tenantSetModuleActivationState(..., true)` and finally changes control-plane status to `active`. Failures remain `pending`; `MigrationRunner`'s `_migrations` ledger makes reruns converge. The P1 migration deliberately has no `IF NOT EXISTS` and was applied/re-run through that ledger on the local tenant DB.
- **Merged micro-gate result:** main `948732a` adds symmetric Entity View `@1` fallback, so P1 exposes only validator-clean `entity.list.post@1` and `entity.get.post@1`; ARK keys remain unversioned. Per approved addendum #2, all 13 non-core suite manifests are explicitly marked `"_enabled": false`, and ModuleManager keeps them dormant.
- **Verification blocker (observed on `948732a`):** `CapabilityAuthorityAuditor::discoverModules()` excludes the 13 disabled manifests, but `scanFiles()` still scans their PHP and `consumerForFile()` then classifies those excluded paths as kernel consumers. Consequently `php tests/capability_authority_audit_test.php` is 17/18 and `php ikabud capability:audit --json` emits seven critical dormant-source findings. `capability:validate` likewise reports dormant dependencies. Reaching the addendum's required 18/18 needs a kernel auditor/validator fix or permission to edit dormant implementation beyond manifest marking; both are outside P1, so the authority gate is blocked without a new decision.
