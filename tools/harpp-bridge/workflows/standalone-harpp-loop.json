{
    "title": "Standalone HARPP: implement -> review -> release-gate",
    "stages": [
        {
            "name": "implement",
            "model": "openai-codex/gpt-5.6-sol",
            "prompt_file": "stages/standalone-implement.md",
            "marker": "SOL_IMPL status=PASS",
            "verify": "git diff --check",
            "negative_control": "rm -rf \"/tmp/harpp-negative-control-git\" && mkdir -p /tmp/harpp-negative-control-git && cd /tmp/harpp-negative-control-git && git init -q && printf 'x\\n' > f && git add f && git -c user.email=nc@example.invalid -c user.name=nc commit -qm init && printf 'x \\n' > f && git diff --check",
            "evidence": "required",
            "timeout": 2400
        },
        {
            "name": "review",
            "model": "openai-codex/gpt-5.6-sol",
            "prompt_file": "stages/standalone-review.md",
            "marker": "SOL_REVIEW status=PASS",
            "verify": "git diff --check",
            "negative_control": "rm -rf \"/tmp/harpp-negative-control-git\" && mkdir -p /tmp/harpp-negative-control-git && cd /tmp/harpp-negative-control-git && git init -q && printf 'x\\n' > f && git add f && git -c user.email=nc@example.invalid -c user.name=nc commit -qm init && printf 'x \\n' > f && git diff --check",
            "evidence": "required",
            "timeout": 1800
        },
        {
            "name": "release-gate",
            "model": "openai-codex/gpt-5.6-sol",
            "prompt_file": "stages/standalone-release-gate.md",
            "marker": "SOL_GATE status=PASS",
            "verify": "git diff --check",
            "negative_control": "rm -rf \"/tmp/harpp-negative-control-git\" && mkdir -p /tmp/harpp-negative-control-git && cd /tmp/harpp-negative-control-git && git init -q && printf 'x\\n' > f && git add f && git -c user.email=nc@example.invalid -c user.name=nc commit -qm init && printf 'x \\n' > f && git diff --check",
            "evidence": "required",
            "timeout": 1800
        }
    ]
}
